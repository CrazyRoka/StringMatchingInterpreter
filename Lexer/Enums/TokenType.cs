namespace Lexer.Enums
{
    public enum TokenType
    {
        Invalid,
        NotDefined,
        Comma,
        OpenParenthesis,
        CloseParenthesis,
        If,
        For,
        Else,
        Number,
        StringValue,
        StatementTerminator,
        SequenceTerminator
    }
}
